# Core data handling
pandas>=2.0
numpy>=1.24

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Machine learning
scikit-learn>=1.3

# Association rule mining (used only if the dataset supports a defensible
# transaction representation — see PROJECT_PLAN.md, Dataset Validation Gate)
mlxtend>=0.22

# Notebooks
jupyter>=1.0
notebook>=7.0

# SQLite is part of the Python standard library (sqlite3) — no extra package needed.
